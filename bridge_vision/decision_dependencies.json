{
  "schema": "bridge-decision-dependencies-v1",
  "dependencies": {
    "faster_whisper": {
      "kind": "model",
      "policy": "Allowed only as raw ASR base. Bridge semantics, corrections and canonical facts must be produced by school-owned post-processing and evidence rules."
    },
    "fitz": {
      "kind": "infrastructure",
      "policy": "PyMuPDF is used only to merge, reopen, render and validate PDF artifacts; it must not infer cards, auctions, evidence provenance or canon status."
    },
    "google": {
      "kind": "infrastructure",
      "policy": "Authentication and Google Drive transport only; must not make bridge-content decisions."
    },
    "psycopg": {
      "kind": "infrastructure",
      "policy": "PostgreSQL queue transport, atomic leasing and receipt persistence only; must not make bridge-content or canonical decisions."
    },
    "requests": {
      "kind": "infrastructure",
      "policy": "HTTP transport only; must not make bridge-content decisions."
    },
    "reportlab": {
      "kind": "infrastructure",
      "policy": "PDF presentation only; it must render supplied evidence classes without creating card, auction, reconstruction or canon decisions."
    },
    "sherpa_onnx": {
      "kind": "model",
      "policy": "Offline structural validation of pinned speaker segmentation and embedding artifacts only; it must not infer bridge content or canonical facts, and any missing, unreadable or incompatible artifact must fail readiness closed."
    }
  }
}
