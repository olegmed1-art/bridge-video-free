# Reconciliation runner repair — 2026-09-21

Status: PRODUCTION RUNNER VERIFIED; automatic re-audit admission verified;
terminal acceptance for the new audits and 24–48h observation remain pending.

## Primary evidence

- Main at investigation: `16468af4d57b23ddc476e3717c400efcf07cd361`.
- Production 0368 checksum matches the handoff:
  `62d0a23a417627a45dadbe540e67ed7475002a92aff6fa4905ec3ebfab93d20c`.
- #1148 audit result comment `5766130677`, exact head
  `8e93cf917c7541177f6ef0eae201454f40fc6af3`, reports `EXACT_HEAD_AUDIT_PASSED`.
- Dispatch `4673788b-167e-42c3-8f83-1fc41639a64f` is `CALLBACK_ACCEPTED`;
  task `ec56537a-3fcc-4c54-b80b-1ffa8b2edb7b` and original work item are `DONE`
  at 2026-09-21 19:19:01 UTC. Exactly one new work-task mapping since 18:00
  and one progress receipt. No repeated command or dispatch-PR merge.
- Active #857 remains PAUSED and dependent #1064 WAITING_DEPENDENCY.

## Runner diagnosis

Read-only Actions run `35647553418`, job `106491613998`, commit
`6f0ce3e3f80dab42999ae79484f91cf554c08cb6` distinguished:

| Probe | Result |
| --- | --- |
| Original DSN plus startup options | STARTUP_PARAMETER_UNSUPPORTED |
| Original DSN without startup options | identity valid; schema missing (3F000) |
| Existing canonical worker DSN without startup options | identity valid; schema denied (42501) |
| Canonical worker DSN plus startup options | STARTUP_PARAMETER_UNSUPPORTED |
| GitHub check-runs with checks:read | PASS |

No DSNs, passwords, response bodies or exception strings were emitted.
The production worker role already has EXECUTE on the four reconciliation RPCs,
but lacks USAGE on the internal schema. Granting internal schema access would
also expose the older non-CAS entrypoint. Migration 0369 instead exposes exactly
four delegating SECURITY DEFINER functions in `autopilot_reconcile`.

## Validation and assurance

- 53 focused Python tests pass: transaction-local timeout order, rollback,
  identity fencing, secret-safe diagnostics, next-step limits and stale PR fences.
- On preserved branch `br-holy-wildflower-b1offf7f`, 0369 was applied and the four
  RPCs executed as `bridge_school_worker`. Both mutation RPCs returned NO_CHANGE
  for a nonexistent work item; direct legacy access was denied.
- Test impersonation uses a SET grant only inside a transaction that ROLLBACKs;
  readback confirmed original admin=true/INHERIT=false/SET=false unchanged.
- Gateway rollback, reapplication and the same role checks passed in a rolled-back
  rehearsal. No branch deletion or production data rewrite.
- The attempted runner canary on this isolated branch, run `35648629008`, failed
  authentication because production credentials do not authenticate to that clone.
  This does not test or disprove the production connection; no passwords changed.
- Independent I2 review (different model) found and resolved stale PR evidence
  and unnecessary credential exposure to dependency installation. Code/SQL PASS;
  runtime acceptance remains contingent on the production gateway preflight.
- Future gateway migrations must explicitly revoke PUBLIC EXECUTE for each new
  function. PostgreSQL per-schema default revocation cannot override the global
  default. The SQL test requires the exact four-function surface and no PUBLIC ACL.

## Promotion and rollback

PR #1761 merged as `1eeaa0aafbea16a8a97e4ad06be210134f92ad49` after all ten
workflows / fourteen checks passed on `97c4cebdbdf770b980f4de5abc597fc050e08dff`.
Fresh main, exact-head CI, schema preflight and the 0368 checksum were verified.
Migration 0369 was applied in production at 2026-09-21 20:12:47 UTC, checksum
`c34a46e263e05de77088b6dd4816756970c0b00ff2faae7e227d9bdf43fec859`.
Readback confirmed gateway USAGE, no internal schema USAGE, no gateway CREATE,
and exactly four gateway functions. No reconciliation RPC was manually invoked
to advance production work.

Five automatic `workflow_run` executions passed on the merged main:
`35649889133`, `35650016448`, `35650020301`, `35650145401`, `35650151615`.
The real-runner preflight proved candidate RPC access, GitHub check-runs access,
and the required privilege restrictions. Both reconciliation steps passed.
Next-step execution remains gated on preflight success.

Rollback callers to their prior revision before removing gateway objects with
`database/rollbacks/0369_autopilot_reconcile_gateway.sql`; no CASCADE is used.
Historical failed callers will still be failed until a corrected release returns.

## Actual automatic progression

[Run 35649889133](https://github.com/olegmed1-art/bridge-video-free/actions/runs/35649889133),
job `106499389011`, admitted three `REAUDIT_READY` transitions using
`TRANSPORT_RECOVERED`. The planner then materialized one task and one dispatch
for each work item. This was automatic production progress, not an isolated test.

| Target | Work item | New task | Dispatch |
| --- | --- | --- | --- |
| #1059 | `d312b2f9-3b6c-4b9f-a7d7-73aa2f915122` | `56f0afaf-fa45-4674-84cd-767a43bdcfab` | `c5e5a372-d342-44bb-97fb-80879d78dca3` |
| #1599 | `12039b10-d7e4-4126-99e1-66df93f257f1` | `eeb5d6d4-ad2f-4206-8970-61ccd58bfef0` | `ee4b4d8a-78a7-4cca-bc85-667eac421216` |
| #1683 | `4b568a11-11f4-47f0-b84f-81de11f58e82` | `b5763a13-5ef8-4386-a8db-c39b051dbfdb` | `f4e6a592-1dfd-4bdf-bd6d-92cf291d84b8` |

At the 20:34–20:41 UTC readback:

- Each case has exactly one progress receipt, one new task and one new dispatch.
  Current GitHub heads match the dispatch bindings: #1059
  `867e6892697bebd17e6509f5a01ff1c08487d649`, #1599
  `dd2669d204cfde7d645acfc069b084176511d6e5`, #1683
  `880543967ffd6ac3905aa80d1edb63db27bafb3c`.
- #1059 and #1599 are SENT with Codex ACKs at 20:21:14 and 20:21:21 UTC.
  Their terminal callback deadlines are 22:21:14 and 22:21:21 UTC.
- #1683 is PUBLISHED without an owner command or ACK; delivery deadline is
  20:45:41 UTC. Its delivery is not yet proven. The reason for the missing
  command is not established.
- All three tasks are WAITING_EXTERNAL. A Codex review summary on #1599 is not
  the required dispatch-bound terminal block and is not accepted as task success.
- Active #857 remains PAUSED/PROVIDER_HOLD; dependent #1064 remains
  WAITING_DEPENDENCY. Both have zero new tasks and zero progress receipts.
- The light production worker is active/running. NRestarts remains 31, unchanged
  from the investigation baseline; its process started on September 19.
- Dispatch PRs #1760 and #1762–#1764 are discovery resources, never merge targets.
  No duplicate owner command was sent during these checks.

A read-only observation task is enabled for the 48-hour window beginning
2026-09-21 20:16 UTC. It tracks terminal callback acceptance, further permitted
transitions, duplicate prevention and protected holds. Reports are due after
24 and 48 elapsed hours; the observation is not complete. A published dispatch
or successful runner alone does not establish full unattended execution.

## Neon cost investigation — deferred by the user

The following is a historical investigation snapshot. The user subsequently
kept the Oracle worker / Neon database architecture and explicitly excluded
sleep, wake and cost checks from the current task and its observation automation.
No migration away from Neon is being performed.

- Production compute `ep-noisy-pine-b1pe30sf`: active; timeout 300 seconds, 0.25–8 CU.
- Separate shadow branch `br-still-tooth-b1ilkfcj`, endpoint
  `ep-floral-field-b1pjs2of`, is active with suspend timeout 0.
- At 19:59 UTC shadow database size 711,204,864 bytes; 181,677 tasks;
  114 new `AUTOPILOT_SMOKE_V1` / `ORACLE_ONLINE_PILOT_V1` tasks in ten minutes.
- Current-period API counters: shadow 810,245 / project 1,763,075 compute seconds,
  approximately 46% of compute consumption, not 46% of the total bill.
- Light host has ~10 GiB available RAM and 38 GiB available root filesystem;
  local PostgreSQL service not active. A separate local shadow PostgreSQL is
  a plausible option, subject to restore verification and cutover planning.
- Production additionally has persistent Autopilot worker connections and
  Assistant Lab control/FinOps traffic. Moving shadow alone cannot prove
  production scale-to-zero. No observed savings, successful sleep/wake cycle,
  or completed 24–48h observation is claimed.
