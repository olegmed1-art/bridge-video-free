# Bounded automatic next steps — 2026-09-21

Governance: ASSURED. Scope: repository audit recovery and dependency progression.
Base reviewed: `c6bde0972ec6cbeb73091dd209f293ae2b9ebe9b`.

## Problem and decision

The older PR #1148 work item remained PAUSED after its head changed because
`EXACT_HEAD_CHECKS_FAILED` had no admitted recovery path. A newer successful audit
belonged to a different lineage and scope; it cannot silently complete that item.
Separately, an audit with findings could become DONE when no repair was admitted,
and a late older task could interfere with a newer generation.

Migration 0368 admits a fresh READ_ONLY audit under the original task specification
when verified head/CI evidence changes. Delivery-contract failures additionally
require a newer accepted delivery on the same route. One receipt per work/head and
three lifetime receipts bound recovery; receipts survive rollback. Prior repair
chains, owner holds, pinned heads and unresolved dependencies remain gated.
Admission is compare-and-set, serialized, and tied to the exact head subsequently
observed by the planner. A different head pauses without creating a task.

Only recognized successful repository audit outcomes complete work and release
dependents. The legacy RPC cannot bypass protected recovery budgets, including
through an unrecognized non-null target disposition. Existing explicit verified
target closure remains available.

The reconciliation workflow uses trusted main, read-only GitHub API access and
bounded database RPCs. It runs after selected CI completions and on the existing
hourly schedule. It never authorizes merge, deploy, production writes by dispatched
tasks, or renewal of exhausted repair chains. No resident worker change is needed.

## Pre-promotion evidence

- 54 focused Python tests passed, including stale/malformed evidence, changing
  head, incomplete check pagination, failed reads and bounded batch behavior.
- Independent Red Team review identified pending-head, legacy budget bypass,
  false DONE, owner-hold and dependency cases; regression tests exercise actual
  PostgreSQL functions and triggers, without disabling those triggers.
- Production-derived isolated Neon branch `br-holy-wildflower-b1offf7f`: 0368
  application and SQL 368 passed. Test fixtures roll back.
- Rollback restored all four original function definitions by database digest
  comparison; reapply and SQL 368 passed in the same rehearsal transaction.
- Existing SQL 325, 347, 348, 353 and 367 passed on the isolated branch.
- SQL 323 assumes a globally empty repair history and fails its repair-count
  assertion on both the unchanged production copy and the patched copy. The full
  database CI runs this regression in a fresh database before promotion.

The separate review is I1. Actual PostgreSQL execution and GitHub CI provide the
independent execution contour required in addition to that review.

## Promotion, rollback and observation

Require current-main reconciliation, exact-head green CI and no unresolved review
blockers. Apply only migration 0368 with its repository checksum recorded. Check
function restoration readiness, privileges, candidate selection and fresh real
lineage before admitting a bounded first batch.

Rollback refuses while admitted work is active or queued. Let it settle or use the
existing owner hold, then run `database/rollbacks/0368_autopilot_next_step.sql`.
It restores original functions and removes the new RPCs, retaining all work, task,
dispatch and retry receipt history. The new runner fails closed when its RPC is
absent; revert the workflow/code if rolling back the release.

Merge and production acceptance are separate from 24–48 hours of observation.
Do not claim sustained autonomy from a single passing audit. Verify successive
permitted steps, protected holds, duplicate suppression and bounded attempts over
that period. PR #857's exhausted repair chain remains held.
