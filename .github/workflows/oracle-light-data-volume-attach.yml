name: Oracle Light data volume attachment
on:
  pull_request:
    paths: ['.github/workflows/oracle-light-data-volume-attach.yml', 'ops/oci_light_data_volume_attach.py']
  push:
    branches: [main]
    paths: ['.github/workflows/oracle-light-data-volume-attach.yml', 'ops/oci_light_data_volume_attach.py']
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: oracle-light-backup-mutation
  cancel-in-progress: false
jobs:
  contract:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
        with:
          persist-credentials: false
      - run: python -m py_compile ops/oci_light_data_volume_attach.py ops/oci_storage_audit.py
      - run: python ops/test_oci_light_data_volume_attach.py
  attach:
    needs: contract
    if: github.repository == 'olegmed1-art/bridge-video-free' && github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
        with:
          persist-credentials: false
      - run: python -m pip install --disable-pip-version-check -q oci==2.186.0
      - name: Attach only the verified existing data volume
        env:
          OCI_USER: ${{ secrets.OCI_CLI_USER }}
          OCI_TENANCY: ${{ secrets.OCI_CLI_TENANCY }}
          OCI_FINGERPRINT: ${{ secrets.OCI_CLI_FINGERPRINT }}
          OCI_KEY: ${{ secrets.OCI_CLI_KEY_CONTENT }}
          OCI_REGION: ${{ secrets.OCI_CLI_REGION }}
        run: python ops/oci_light_data_volume_attach.py
