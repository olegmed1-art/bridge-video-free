name: Oracle Light autopilot routing lease

on:
  pull_request:
    paths: ['.github/workflows/oracle-light-route-lease.yml', 'ops/oracle_light_route_install.py', 'ops/oracle_light_route_lease.py', 'ops/oracle_light_route_probe.py', 'ops/github_autopilot_db_route.py', 'ops/test_oracle_light_route_lease.py', 'ops/test_autopilot_github_db_route.py', 'ops/test_autopilot_cutover_routing_inventory.py', '.github/workflows/autopilot-reconcile-diagnostic.yml', '.github/workflows/autopilot-chatgpt-role-callback.yml', '.github/workflows/autopilot-codex-event-callback.yml', '.github/workflows/autopilot-paused-reconcile.yml', '.github/workflows/autopilot-mailbox-pre-rotation.yml', '.github/workflows/database-health-monitor.yml']
  push:
    branches: [main]
    paths: ['.github/workflows/oracle-light-route-lease.yml', 'ops/oracle_light_route_install.py', 'ops/oracle_light_route_lease.py', 'ops/oracle_light_route_probe.py', 'ops/github_autopilot_db_route.py', 'ops/test_oracle_light_route_lease.py', 'ops/test_autopilot_github_db_route.py', 'ops/test_autopilot_cutover_routing_inventory.py', '.github/workflows/autopilot-reconcile-diagnostic.yml', '.github/workflows/autopilot-chatgpt-role-callback.yml', '.github/workflows/autopilot-codex-event-callback.yml', '.github/workflows/autopilot-paused-reconcile.yml', '.github/workflows/autopilot-mailbox-pre-rotation.yml', '.github/workflows/database-health-monitor.yml']
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: oracle-light-backup-mutation
  cancel-in-progress: false

jobs:
  contract:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
        with:
          persist-credentials: false
      - run: python -m py_compile ops/oracle_light_route_install.py ops/oracle_light_route_lease.py ops/oracle_light_route_probe.py ops/github_autopilot_db_route.py
      - run: python ops/test_oracle_light_route_lease.py
      - run: python ops/test_autopilot_github_db_route.py
      - run: python ops/test_autopilot_cutover_routing_inventory.py
      - name: Validate shell syntax
        run: |
          ruby -ryaml -e 'd=YAML.load_file(ARGV[0]); d.fetch("jobs").each_value { |j| j.fetch("steps").each { |s| next unless s["run"]; IO.popen(["bash", "-n"], "w") { |p| p.write(s["run"]) }; abort "invalid shell" unless $?.success? } }' .github/workflows/oracle-light-route-lease.yml

  tunnel:
    needs: contract
    if: github.repository == 'olegmed1-art/bridge-video-free' && github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
        with:
          persist-credentials: false
      - name: Install fixed routing command and prove lease drain
        env:
          SSH_PRIVATE_KEY: ${{ secrets.ORACLE_SSH_PRIVATE_KEY }}
        shell: bash
        run: |
          set -Eeuo pipefail
          umask 077
          work=$(mktemp -d)
          trap 'rm -rf "$work"' EXIT
          if [[ -z "${SSH_PRIVATE_KEY:-}" ]]; then
            echo 'LIGHT_SSH_KEY=ABSENT'
            exit 1
          fi
          printf '%s\n' "$SSH_PRIVATE_KEY" | tr -d '\r' > "$work/key"
          unset SSH_PRIVATE_KEY
          if ! ssh-keygen -y -P '' -f "$work/key" > "$work/public" 2>/dev/null; then
            echo 'LIGHT_SSH_KEY=INVALID_OR_ENCRYPTED'
            exit 1
          fi
          echo 'LIGHT_SSH_KEY=VALID'
          bash ops/oracle_known_hosts_from_scan.sh 92.5.47.149 SHA256:XBR1x74uJ41BxmDF7Y9P20GjIjNbrYXqieV4c2MC0Go "$work/known_hosts"
          echo 'LIGHT_SSH_HOST=VERIFIED_AGAINST_OWNER_ACCEPTED_FINGERPRINT'
          python ops/oracle_light_route_install.py bundle > "$work/install.py"
          if ! ssh -F /dev/null -i "$work/key" -o BatchMode=yes -o IdentitiesOnly=yes -o StrictHostKeyChecking=yes -o UserKnownHostsFile="$work/known_hosts" -o ConnectTimeout=15 -o ConnectionAttempts=1 -o ServerAliveInterval=10 -o ServerAliveCountMax=2 ubuntu@92.5.47.149 'sudo -n /usr/bin/python3 -' < "$work/install.py" > "$work/result" 2> "$work/error"; then
            cat "$work/result"
            echo 'LIGHT_PG_TUNNEL=FAILED'
            if grep -q 'Permission denied' "$work/error"; then echo 'LIGHT_SSH_FAILURE=AUTHORIZATION'; else echo 'LIGHT_SSH_FAILURE=TRANSPORT_OR_REMOTE_COMMAND'; fi
            exit 1
          fi
          cat "$work/result"
          ssh_opts=(-F /dev/null -i "$work/key" -o BatchMode=yes -o IdentitiesOnly=yes -o StrictHostKeyChecking=yes -o UserKnownHostsFile="$work/known_hosts" -o ConnectTimeout=15 -o ServerAliveInterval=10 -o ServerAliveCountMax=2)
          ssh "${ssh_opts[@]}" ubuntu@92.5.47.149 'sudo -n /usr/bin/cat /etc/bridge-autopilot-postgres/ca.crt' > "$work/ca.crt"
          python ops/oracle_light_pg_tunnel_probe.py "$work"
          python ops/oracle_light_route_probe.py "$work"
