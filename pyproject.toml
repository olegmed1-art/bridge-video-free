[project]
name = "bridge-school-api-runtime"
version = "0.1.0"
description = "Minimal production API runtime for the School of Sports Bridge"
requires-python = ">=3.12,<3.13"
dependencies = [
  "fastapi==0.141.1",
  "psycopg[binary]==3.3.4",
]
